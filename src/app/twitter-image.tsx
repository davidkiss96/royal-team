/** Reuses the exact same generated image as `opengraph-image.tsx` for
 * Twitter/X's `summary_large_image` card — one generator, not a second,
 * duplicated image-generation implementation. */
export { default, alt, size, contentType } from "./opengraph-image";
